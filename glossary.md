# 용어집

> **골격 문서다.** 레포 경계에서 자주 오해되는 용어만 먼저 담았다.
> `archive/IA.md`·`archive/AA.md`의 용어는 **미증류**이며 Phase D에서 채운다.

## 조직 · 사람

| 용어 | 뜻 | 주의 |
|---|---|---|
| **tenant** | 조직. 모든 데이터의 테넌트 경계 | 코드·DB에서는 `tenant`, 제품 화면에서는 "조직" |
| **회사** | tenant(조직)의 통칭 — 특히 telemetryctl 문서의 "회사 manifest"·"회사 Collector" | tenant와 같은 뜻이다 |
| **team** | 팀. **약속하는 최소 집계 단위** | 프로젝트 축은 Non-goal |
| **member** | 조직 구성원 | 사람이다. `installation`과 다르다 |
| **installation** | 사용자 PC에 설치된 인스턴스. 데스크탑의 **인증 주체** | 사람 계정이 아니다. 한 member가 여러 installation을 가질 수 있다 |
| **as-of 조인** | 이벤트 **발생 시각 기준**으로 팀 소속을 판정하는 것 | 현재 소속으로 과거 이벤트를 귀속하지 않는다 (D-3) |

## 자격증명

| 용어 | 뜻 | 주의 |
|---|---|---|
| **초대 코드** | `XXXX-XXXX-XXXX`. 관리자가 발급하는 일회성 코드 | 무염 SHA-256으로 해시 저장. **원본은 발급 응답에서 한 번만 나간다** |
| **`installation_token` (`pit_`)** | 이 설치의 장기 신원 | OS 키링에만. 교체하지 않는다 |
| **`telemetry_token` (`ptt_`)** | 텔레메트리 전송용 | OS 키링에. **해시만 HMAC-SHA256** — 다른 둘과 다르다 |
| **로컬 ingest 토큰** | 벤더 도구 → 데몬 로컬 수신기 인증용 | **`ptt_`와 다른 값이다.** 벤더 설정 파일에 들어가는 건 이쪽 |
| **manifest** | 회사 단위 OTel 설정(엔드포인트·signals·privacy) | 자격증명을 담지 않는다 (봉투 분리) |

## 파이프라인

| 용어 | 뜻 | 주의 |
|---|---|---|
| **signal** | OTel 신호(logs·metrics·traces) 한 건 | |
| **raw** | 이 제품에서는 **가공 전** 시그널을 뜻한다 | **마스킹 전 원본이 아니다.** Raw Object Storage의 데이터는 이미 마스킹을 마쳤다 |
| **Collector** | OTLP 수신 노드 | **한 낱말이 셋을 가리킨다** — 대문자 `Collector`는 이 노드, 소문자 `collector`는 현행 구현인 OTel Collector 컨테이너(ADR 0005가 걷어낸다), `telemetry-collector`는 이관 후 backend 모듈이다 |
| **Masker** | 서버 측 2차 마스킹. 조직별 규칙 적용 | 데몬의 1차 집행과 짝. **현재**는 OTel Collector 컨테이너의 전역 `redaction`이 부분 구현하고, **이관 후**에는 backend `telemetry-collector` 모듈의 `masking` 패키지가 맡는다. 이름이 닮았을 뿐 다른 런타임이다 |
| **Adapter** | 벤더별 OTel 형식 → 내부 공통 스키마 변환 + **공시 단가 기준 금액** 환산 | backend 모듈 `telemetry-adapter`. 벤더별 매핑은 그 안의 `source` 패키지다 |
| **Enricher** | 조직·팀·사용자 결합 + 시간 집계 후 적재 | **Signal DB에 쓰는 유일한 주체** (I-4). backend는 결합(`telemetry-enricher`)과 적재(`telemetry-persistence`) 두 모듈로 구현하며 쓰기 소유는 후자다 |
| **post-processor** | `ai-telemetry-pipeline`의 `telemetry-processor` 앱을 부르는 컨테이너·ECR 이름 | 같은 앱이다 — infra 문서는 post-processor, 파이프라인 레포는 telemetry-processor |
| **재처리** | Object Storage → Adapter부터 다시 태우는 운영 개입 경로 | Masker를 다시 태우지 않는다 (I-9). 멱등해야 한다 (I-10) |

## 비용 — ★ 섞으면 안 되는 두 값

| 용어 | 뜻 | 계산 시점 |
|---|---|---|
| **공시 단가 기준 금액** | 벤더가 공표한 토큰당 단가로 환산한 금액. **조직마다 다르지 않다** | 적재 시(Adapter), Signal DB에 저장 |
| **계약 적용 금액** | 조직이 맺은 계약 단가를 적용한 실비용 | **조회 시**(Dashboard API) |

두 값은 **이름을 구분해 표시하고 섞지 않는다**(제품 원칙 3).
금액과 함께 **단가 버전**을 저장하는 이유는 공시 단가 개정 시 재처리로 소급 정정하기 위해서다.

## 시나리오

| 용어 | 뜻 | 주의 |
|---|---|---|
| **시나리오 런처** | 관리자가 질문을 고르면 그 답이 있는 화면·필터를 적용해 주는 표면 | **네비게이션 보조다.** 지표를 해석하거나 조언하지 않는다 |
| **시나리오 카탈로그** | 질문·필요 지표·대상 화면·선행 조건의 목록. 전역 참조 데이터 | **가용성을 담지 않는다** |
| **가용성 3단계** | 이 조직에서 이 질문에 답할 수 있는가의 판정 | 저장하지 않고 **조회 시 판정** (I-14) |
| **지표 레지스트리** | 카탈로그가 참조할 수 있는 지표 ID 목록 | 시나리오는 새 지표를 만들지 않는다 (I-13) |
| **어드바이스 엔진** | 지표를 해석해 조언을 만드는 것 | **Non-goal.** 시나리오 런처와 같은 것으로 취급하지 않는다 |

## 관측성

| 용어 | 뜻 | 주의 |
|---|---|---|
| **운영 로그** | 각 컴포넌트가 자기 처리 상태를 내보내는 것(CloudWatch·Sentry) | 데이터 평면이 아니다 |
| **감사 로그** | 누가 무엇을 조회·변경했는가의 기록 | **범위 밖이다.** 운영 로그가 이것을 대체하지 않는다 |
